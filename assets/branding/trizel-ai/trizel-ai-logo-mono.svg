<svg xmlns="http://www.w3.org/2000/svg"
     viewBox="0 0 360 120"
     role="img"
     aria-label="TRIZEL-AI">
  <title>TRIZEL-AI</title>

  <defs>
    <style>
      .wordmark {
        font-family: ui-sans-serif, system-ui, -apple-system, "Segoe UI",
                     Roboto, Arial, "Noto Sans", "Liberation Sans", sans-serif;
        font-weight: 800;
        font-size: 64px;
        letter-spacing: 0.06em;
        fill: currentColor;
      }
      .accent {
        fill: currentColor;
        opacity: 0.22;
      }
      .rule {
        stroke: currentColor;
        stroke-width: 6;
        opacity: 0.22;
      }
    </style>
  </defs>

  <!-- Institutional accent -->
  <rect x="18" y="26" width="10" height="70" rx="2" class="accent"/>
  <rect x="18" y="26" width="70" height="10" rx="2" class="accent"/>

  <!-- Wordmark -->
  <text x="100" y="82" class="wordmark">TRIZEL-AI</text>

  <!-- Baseline rule -->
  <line x1="100" y1="98" x2="338" y2="98" class="rule"/>
</svg>
